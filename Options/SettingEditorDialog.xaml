<Window x:Class="GoToRecentFile.Options.SettingEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Setting Editor"
        Width="420"
        Height="220"
        MinWidth="320"
        MinHeight="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Name -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" IsReadOnly="True" Margin="0,0,0,8"
                 Padding="4,2" VerticalContentAlignment="Center"
                 Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>

        <!-- Value -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Value:" VerticalAlignment="Top" Margin="0,0,8,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" x:Name="ValueBox"
                 AcceptsReturn="False" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Padding="4,2" VerticalContentAlignment="Top"/>

        <!-- Error message -->
        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                   x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap"
                   Margin="0,6,0,0" Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Height="24" Margin="0,0,8,0" IsDefault="True" Click="OK_Click"/>
            <Button Content="Cancel" Width="80" Height="24" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
